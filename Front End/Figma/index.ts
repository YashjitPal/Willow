/**
 * Willow Figma — public entry. The Dashboard lazy-loads this module for the
 * "Figma" sidebar tab; nothing else should be imported from outside.
 */

export { FigmaWorkspace } from './FigmaWorkspace';
export { default } from './FigmaWorkspace';
