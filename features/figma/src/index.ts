/**
 * Willow Figma — public entry. Nothing else should be imported from outside
 * this folder.
 *
 * UNFINISHED: this feature does not compile and is not routed yet. See
 * ../README.md.
 */

export { FigmaWorkspace } from './FigmaWorkspace';
export { default } from './FigmaWorkspace';
