{
  "//": "Kept minimal: the shared compiler options and the @willow/* map live in the repo-root tsconfig.base.json. Editors pick this up for files under apps/studio.",
  "extends": "../../tsconfig.base.json",
  "include": ["src", "test", "scripts", "vite.config.ts"],
  "exclude": ["node_modules", "dist"]
}
